# Lab9.2 – Character Counter

This project is a simple web application built with **TypeScript** and **Vite**.  
It counts the number of characters entered in a text box and displays the result in real time.

## 🚀 Features
- Real-time character counting
- Clean UI with Bootstrap
- Easy to extend and customize

## 📂 Project Structure
- `src/` → TypeScript source files
- `public/` → Static assets
- `tsconfig.json` → TypeScript configuration
- `vite.config.ts` → Vite build configuration

## ▶️ How to Run
```bash
npm install
npm run dev
